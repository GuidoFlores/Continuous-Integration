on:
  pull_request:
    branches:
      - main

jobs:
  build:
    runs-on: windows
    steps:
      - uses: actions/setup-python@v5
      - name: Setup Node
        with:
          node-version: '20.x'
